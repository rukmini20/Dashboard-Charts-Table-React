const baseURL = "https://freddy.codesubmit.io";

const login = "/login";
const dashboard = "/dashboard";
const refresh = "/refresh";
const orders = "/orders";

export { baseURL, login, dashboard, refresh, orders};
